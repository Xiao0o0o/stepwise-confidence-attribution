# Python 3.10 (tested on 3.10.16)
#
# On the cluster the working environment is:
#   module load mamba
#   mamba create -n reasoning python=3.10
#   mamba activate reasoning
#   pip install -r requirements.txt
#
# Versions below are pinned to the exact set this project was tested with.
# torch / vllm are CUDA builds — install them on a GPU node whose CUDA matches
# (these wheels target CUDA 12.x). Stages 2, 3 (--device cpu) and 6 run without a GPU.

# --- Core (CPU; enough for Stage 2 transform + Stage 6 metrics) ---
numpy==1.26.4
scipy==1.15.3
scikit-learn==1.7.1
matplotlib==3.10.5
pandas==2.3.0
tqdm==4.67.1
networkx==3.4.2

# --- Deep learning / models ---
torch==2.5.1
transformers==4.51.3
tokenizers==0.21.1
accelerate==1.6.0
huggingface-hub==0.30.2
sentencepiece==0.2.0          # tokenizers for BERT / DeBERTa

# --- Stage 1 generation only (needs a CUDA GPU) ---
vllm==0.6.6.post1

# --- Stage 4 LLM-judge labeling ---
openai==1.75.0
